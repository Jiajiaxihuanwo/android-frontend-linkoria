<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".root.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Androidfrontendlinkoria"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".server.ui.CreateServerActivity"
            android:exported="false" />
        <activity
            android:name=".server.ui.JoinServerActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>
        <activity
            android:name=".chat.ui.ChatActivity"
            android:exported="false" />
        <activity
            android:name=".root.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".auth.ui.AuthActivity"
            android:exported="false" />
        <activity
            android:name=".root.MainActivity"
            android:exported="false" />
    </application>

</manifest>